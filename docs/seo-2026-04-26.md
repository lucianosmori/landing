# SEO + AI search audit — 2026-04-26

Snapshot taken on the v2 branch right after the v2.0.0 redesign shipped. Source GSC data: `Last 3 months` ending 2026-04-26 for `sc-domain:lucianomori.cloud`.

## What landed on `v2` (on-page + infra)

| Area | Before | After |
|---|---|---|
| `robots.txt` | 404 | Allows all + explicit allows for GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot. Points at sitemap. |
| `sitemap.xml` | 404 | Single homepage entry. |
| `llms.txt` | 404 | Spec-aligned (llmstxt.org) — primer line, linked bullets, Optional section. |
| `<link rel="canonical">` | missing | `https://lucianomori.cloud/` |
| `<meta name="robots">` | missing | `index, follow, max-image-preview:large, max-snippet:-1` |
| `og:url` | stale `lucianomori.tech` in indexed copy | `https://lucianomori.cloud/` in current source — refreshes on next crawl |
| `og:image` | stale GitHub raw blob hash | self-hosted at `lucianomori.cloud/images/blackhat.jpeg` |
| `og:type` | `website` | `profile` (with first/last name) |
| `article:published_time` / `article:modified_time` | missing | added (2026-04-26), satisfies LinkedIn date warning |
| Twitter Card | missing | `summary_large_image` |
| Structured data | none | JSON-LD `@graph` with `Person`, `WebSite`, `ProfilePage`, `knowsAbout`, `sameAs` to LinkedIn / GitHub / Credly |
| Footer dangling `<a>` | 3 anchors no href | `<span class="footer-tag">` |

## Run on 2026-04-26 (browser-driven, claude.ai extension)

| Step | Status | Notes |
|---|---|---|
| Submit `sitemap.xml` to GSC | ✅ Done | "Sitemap submitted successfully" |
| Indexing → Pages → Not indexed → Request indexing | ⚠️ Skipped intentionally | Only 2 URLs in that bucket: `http://lucianomori.cloud/` and `http://www.lucianomori.cloud/` — both correctly redirecting to canonical https. Re-requesting would burn daily quota for nothing. |
| URL Inspection on `/` → Test live URL → confirm structured data | ✅ Done | URL available, indexable, 1 valid Profile-page item detected |
| URL Inspection → Request Indexing | ✅ Done | Added to priority crawl queue |
| Rich Results Test (search.google.com/test/rich-results) | ✅ Done | 1 valid item, 0 errors, 0 warnings — Profile page for "Luciano S. Mori" |
| LinkedIn Post Inspector | ✅ Done | HTTP 206, OG metadata parsed; cache refreshed |
| Twitter / X Card Validator | ❌ Blocked | Validator now requires x.com login; do this one manually at https://cards-dev.x.com/validator |

## What the GSC CSVs say (Last 3 months, ending 2026-04-26)

- **9 clicks / 79 impressions / 11.39% CTR / pos 4.08** on a single indexed page. CTR is healthy at that position; the bottleneck is **impressions volume**, not CTR.
- **Only 2 queries surfacing**: `luciano mori` (4 / 25, pos 3.76) and `mori cloud` (0 / 1, pos 7). Branded only. Topical surfacing should follow the structured data + `llms.txt` over 4–8 weeks.
- **Mobile is 60% of impressions, 67% of clicks** (45/6 vs desktop 33/3).
- **Geo asymmetry**: 21 countries get impressions, only Canada clicks (9/9).
- **"Search appearance"** still empty pre-deploy → unlocks now that JSON-LD is live and indexing is requested.
- **"2 not indexed pages"** are http→https redirect variants. Expected, not actionable.

## Items still on the user

1. **Twitter / X Card Validator**: log in at https://cards-dev.x.com/validator and paste `https://lucianomori.cloud/` to refresh X's preview cache.
2. (Optional) Open `https://lucianomori.cloud/sitemap.xml` in a browser to confirm it lists every page expected (currently just the homepage — by design for v2, but if project pages are added later they need to be appended).
3. After ~7 days, re-pull the GSC Performance CSV and check whether topical queries (DevSecOps, AWS, Black Hat, agentic AI) start appearing.

## Service account guide (for automated GSC pulls — overkill for now)

Kept here for when traffic justifies it. ~10 minutes one-time.

1. **Create a Google Cloud project** at https://console.cloud.google.com (e.g. `lucianomori-site-ops`).
2. **Enable the Search Console API**: *APIs & Services → Library* → search "Search Console API" → **Enable**.
3. **Create the service account**: *APIs & Services → Credentials → Create Credentials → Service account*. Name `gsc-reader`, no project IAM role needed.
   - Click into the service account → *Keys → Add key → JSON*. Save the file.
   - Note the email — `gsc-reader@<project-id>.iam.gserviceaccount.com`.
4. **Grant GSC access**: GSC → property → *Settings → Users and permissions → Add user*. Email = the service account email. Permission **Restricted** for analytics, **Full** if you also want it to submit sitemaps.
5. **Use it**:
   - **GitHub Action**: store the JSON key as a repo secret `GSC_SA_KEY`; weekly Action runs Python (`google-api-python-client`) calling `webmasters.searchanalytics.query` with dimensions `["query","page","country","device"]`, writes a CSV to `data/gsc-YYYY-MM.csv`, commits it.
   - **Local script**: same script, run ad-hoc, paste the CSV here.

**Security**
- Add `*.json` and `service-account*.json` to `.gitignore`.
- Rotate the key annually.
- The service account only has read access to the one GSC property.

## Live interaction option (Chrome DevTools MCP)

Installed Google's official Chrome DevTools MCP (`npx chrome-devtools-mcp@latest`) at user scope. To attach it to a logged-in Chrome (PowerShell-correct command):

```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="$env:TEMP\chrome-mcp-profile"
```

(The `&` call operator is required because the path contains spaces. `$env:TEMP` is the PowerShell-native env var; `%TEMP%` is cmd-style and won't expand here.)

Then sign in to Google in that window, restart Claude Code, and ask Claude to use the `chrome-devtools` MCP at `--browser-url=http://localhost:9222`.
